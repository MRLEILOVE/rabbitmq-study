<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.leigq.shardingspherestudy.domain.mapper.TestMapper">

    <select id="countAge" resultType="java.lang.Integer">
        select count(age) from test;
    </select>

    <select id="listTest" resultType="Test">
        select * from test;
    </select>
</mapper>